logging:
  default-level: "INFO"
  level:
    com.pixelo.health: debug
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true